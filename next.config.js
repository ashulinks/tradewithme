/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', // required for next export in Next.js 13+
  // No experimental keys like appDir or swcMinify
};

module.exports = nextConfig;
